import React from "react";

function Products() {
  return <div style={styles.main}>Products</div>;
}

const styles = {
  main: {
    width: "100vw",
    height: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    color: "white",
  },
};

export default Products;
